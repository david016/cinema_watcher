# Python
__pycache__/
*.py[cod]

# Logy skriptu (v GitHub Actions beží s --log-file "", takže tam nevznikajú)
cinema_watcher.log
cinema_watcher.log.*

# Surové odpovede z API (--dump-raw)
dump/

# Netlify / Node
.netlify/
node_modules/
.env

# Lokálne behy cez run-local.ps1 — vlastný stav, log a .env, aby si nekolidovali
# so stavom, ktorý do repa commituje GitHub Actions.
local/

# Osobné nastavenia Claude Code (platia len na tomto
  stroji)
.claude/settings.local.json
